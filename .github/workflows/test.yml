name: Simple IF Example

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Build
      run: echo "Build successful"

    - name: Deploy
      if: success()
      run: echo "Deploying..."
